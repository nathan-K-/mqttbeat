- key: message
  title: "Mqtt message"
  description: >
    A message from a mqtt client
  fields:
    - name: topic
      type: keyword
      required: true
      description: >
        The topic corresponding to the message, basically an url
